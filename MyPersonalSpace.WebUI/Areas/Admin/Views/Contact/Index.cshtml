@{
    Layout = "_AdminLayout";
}
<link rel="stylesheet" href="https://cdn.datatables.net/1.10.24/css/jquery.dataTables.min.css" />
<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script src="https://cdn.datatables.net/1.10.24/js/jquery.dataTables.min.js"></script>

@model List<ResultContactDto>

<section class="blog-list px-3 py-5 p-md-5">
    <h1>Mesajlar</h1>
    <div class="container single-col-max-width">
        <table class="table table-striped table-bordered" id="contactTable">
            <thead class="table-dark">
                <tr>
                    <th>#</th>
                    <th>İsim</th>
                    <th>Email</th>
                    <th>Konu</th>
                    <th>Mesaj</th>
                    <th>Tarih</th>
                    <th>Güncelle</th>
                    <th>Sil</th>
                </tr>
            </thead>
            <tbody>
                @if (Model != null && Model.Any())
                {
                    int count = 1;
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@count</td>
                            <td>@item.Name</td>
                            <td>@item.Email</td>
                            <td>@item.Subject</td>
                            <td>@item.Message</td>
                            <td>@item.CreatedAt</td>
                            <td>
                                <a class="btn btn-warning btn-sm" href="/Admin/Contact/UpdateContact/@item.ContactId">Güncelle</a>
                            </td>
                            <td>
                                <a class="btn btn-danger btn-sm" href="/Admin/Contact/DeleteContact/@item.ContactId">Sil</a>
                            </td>
                        </tr>
                        count++;
                    }
                }
                else
                {
                    <tr>
                        <td colspan="8" class="text-center">Henüz hiç gönderi bulunmamaktadır.</td>
                    </tr>
                }
            </tbody>
        </table>

        <script>$(document).ready(function () {
                $('#contactTable').DataTable();
            });</script>
        <nav class="blog-nav nav nav-justified my-5">
            <a class="nav-link-prev nav-item nav-link d-none rounded-left" href="#">Previous<i class="arrow-prev fas fa-long-arrow-alt-left"></i></a>
            <a class="nav-link-next nav-item nav-link rounded" href="#">Next<i class="arrow-next fas fa-long-arrow-alt-right"></i></a>
        </nav>

        <a class="btn btn-info" href="/Admin/Contact/AddContact">Post Ekle</a>
    </div>
</section>
